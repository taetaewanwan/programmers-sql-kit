-- 코드를 작성해주세요
SELECT CONCAT(QUARTER(DIFFERENTIATION_DATE), 'Q') AS QUARTER, COUNT(*) AS ECOLI_COUNT
    FROM ECOLI_DATA
    GROUP BY QUARTER
    ORDER BY QUARTER;
    
-- MYSQL에서는 SELECT에서 선언한 별칭 GROUP BY랑 ORDER BY에서 사용 가능